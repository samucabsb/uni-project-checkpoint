import jwt from 'jsonwebtoken';export type TokenPayload={id_usuario:number;nm_usuario:string;tipo_usuario:string};export const generateToken=(p:TokenPayload)=>jwt.sign(p,process.env.JWT_SECRET||'secret',{expiresIn:(process.env.JWT_EXPIRES_IN||'7d') as jwt.SignOptions['expiresIn']});export const verifyToken=(t:string)=>jwt.verify(t,process.env.JWT_SECRET||'secret') as TokenPayload;
